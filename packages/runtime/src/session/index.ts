/**
 * Decision Session barrel — canonical public API only (ED-DA-03).
 *
 * Pipeline composers and low-level helpers: `@embed-engine/runtime/testing`
 * (or relative `./testing` inside this package).
 *
 * @see OWNERSHIP.md
 * @see public-api.ts
 */

export * from "./public-api";
