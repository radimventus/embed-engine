/**
 * AI Delivery boundary (AID-01).
 *
 * WP-A placeholder — no orchestration, capability negotiation, or Adapter
 * dispatch yet (CAP-AI-DELIVERY-01 Steps B–C / CAP-AI-ADAPTER-01).
 *
 * Future home for:
 * - Delivery port (ACC-01 complete / stream)
 * - Capability negotiation
 * - Adapter dispatch (Direct Adapter mode today)
 */

export const AI_DELIVERY_BOUNDARY = "AID-01/delivery" as const;
