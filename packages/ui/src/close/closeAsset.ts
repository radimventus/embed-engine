/**
 * Platform close asset — single PNG for all Close actions.
 * Replace this file to update the icon everywhere.
 */

import closeUrl from '../assets/icons/close.png';

/** Display size of the PNG (px). */
export const CLOSE_ICON_DISPLAY_PX = 30;

/** Minimum clickable area (px). */
export const CLOSE_HIT_AREA_PX = 44;

/** Shared close PNG URL — do not duplicate this asset elsewhere. */
export const CLOSE_ICON_SRC = closeUrl;
