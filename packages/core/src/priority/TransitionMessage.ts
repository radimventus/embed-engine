/**
 * Priority Domain Model §2.15 — TransitionMessage.
 *
 * Short bridge copy (1–2 sentences); adds no new meaning.
 * Ephemeral Journey chrome (Integration Model OQ-05 Resolved).
 */

export type TransitionMessage = {
  readonly text: string;
};
