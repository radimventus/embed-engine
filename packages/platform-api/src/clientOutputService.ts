import type { ClientOutputSnapshot, ClientOutputTrigger } from '@embed-engine/document-runtime';
import type { ClientOutputDelivery } from './clientOutputDelivery';
import type { ClientOutputRepository, DurableClientOutput } from './clientOutputRepository';
export async function issueClientOutput(input:{documentId:string;createdAt:string;projectId:string;houseId:string;recipient:string;trigger:ClientOutputTrigger;auditLeadId:string|null;snapshot:ClientOutputSnapshot;pdf:Uint8Array},repository:ClientOutputRepository,deliver:ClientOutputDelivery):Promise<DurableClientOutput>{let record=await repository.create({documentId:input.documentId,createdAt:input.createdAt,projectId:input.projectId,houseId:input.houseId,knowledgeVersion:input.snapshot.knowledgeVersion,recipient:input.recipient,trigger:input.trigger,auditLeadId:input.auditLeadId,snapshot:input.snapshot,pdfBase64:Buffer.from(input.pdf).toString('base64'),deliveryStatus:'PENDING',deliveryProviderId:null,deliveryError:null});const outcome=await deliver(record);record=await repository.updateDelivery(record.documentId,{deliveryStatus:outcome.status,deliveryProviderId:outcome.providerId??null,deliveryError:outcome.error??null});return record;}
