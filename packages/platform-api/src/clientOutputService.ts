import type { ClientOutputSnapshot, ClientOutputTrigger } from '@embed-engine/document-runtime';
import type { ClientOutputDelivery } from './clientOutputDelivery';
import type { ClientOutputRepository, DurableClientOutput } from './clientOutputRepository';
export type ClientOutputCreateInput={documentId:string;createdAt:string;projectId:string;houseId:string;recipient:string|null;trigger:ClientOutputTrigger;auditLeadId:string|null;snapshot:ClientOutputSnapshot;pdf:Uint8Array};
export async function persistClientOutput(input:ClientOutputCreateInput,repository:ClientOutputRepository):Promise<DurableClientOutput>{return repository.create({documentId:input.documentId,createdAt:input.createdAt,projectId:input.projectId,houseId:input.houseId,knowledgeVersion:input.snapshot.knowledgeVersion,recipient:input.recipient,trigger:input.trigger,auditLeadId:input.auditLeadId,snapshot:input.snapshot,pdfBase64:Buffer.from(input.pdf).toString('base64'),deliveryStatus:input.recipient===null?'NOT_CONFIGURED':'PENDING',deliveryProviderId:null,deliveryError:input.recipient===null?'RECIPIENT_NOT_REQUESTED':null});}
export async function deliverPersistedClientOutput(record:DurableClientOutput,repository:ClientOutputRepository,deliver:ClientOutputDelivery):Promise<DurableClientOutput>{const outcome=await deliver(record);return repository.updateDelivery(record.documentId,{deliveryStatus:outcome.status,deliveryProviderId:outcome.providerId??null,deliveryError:outcome.error??null});}
export async function issueClientOutput(input:ClientOutputCreateInput,repository:ClientOutputRepository,deliver:ClientOutputDelivery):Promise<DurableClientOutput>{const record=await persistClientOutput(input,repository);return deliverPersistedClientOutput(record,repository,deliver);}
