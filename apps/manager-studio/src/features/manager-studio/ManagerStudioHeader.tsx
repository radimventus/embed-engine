/**
 * @deprecated EPIC-BX-11 — Platform Header lives in `@embed-engine/platform-shell`.
 * Kept as a named re-export for historical MSCB-01 references.
 */
export { PlatformHeader as ManagerStudioHeader } from '@embed-engine/platform-shell';
