# CAP-OP-09 — Mail Transport MVP (kontakt@conis.cz)
# Used by Node mail adapters only — never by Office UI.

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

IMAP_HOST=
IMAP_PORT=993
IMAP_SECURE=true
IMAP_USER=
IMAP_PASSWORD=
