/**
 * Platform gateway contracts (EPIC-BLD-06).
 * Interfaces only — no cloud / Runtime / storage implementations.
 */

export type {
  PublishGateway,
  RuntimeGateway,
  StorageGateway,
} from '../../model';
