import assert from'node:assert/strict';import test from'node:test';import type{ClientOutputSnapshot}from'@embed-engine/document-runtime';import{submitClientOutput}from'./clientOutputClient';
const snapshot={schemaVersion:1,capturedAt:'2026-09-14T08:00:00Z',company:{id:'c',name:'C'},project:{id:'p',name:'P'},house:{id:'h',name:'H',storeys:1},knowledgeVersion:'v04',priorities:[],exterior:[],floorPlans:[],interiors:[],priorityNarratives:[],connectedTopics:[],blindspots:[],faq:[],plotAndProcess:[],auditConclusion:'A',cta:'C'}satisfies ClientOutputSnapshot;
test('both triggers use the one client output delivery endpoint',async()=>{for(const trigger of['HEADER','AUDIT']as const){let payload:Record<string,unknown>|null=null;const result=await submitClientOutput({snapshot,recipient:'client@example.test',trigger,...(trigger==='AUDIT'?{auditLeadId:'lead-1'}:{})},async(_url,init)=>{payload=JSON.parse(String(init?.body));return new Response(JSON.stringify({documentId:'doc',createdAt:'2026-09-14T08:00:00Z',deliveryStatus:'SENT',deliveryError:null}),{status:201});});assert.equal(payload?.trigger,trigger);assert.equal(result.documentId,'doc');}});
test('does not report success when the server rejects persistence',async()=>{await assert.rejects(()=>submitClientOutput({snapshot,recipient:'client@example.test',trigger:'HEADER'},async()=>new Response('{}',{status:500})));});
