/**
 * Legacy presentation-assets adapter removed (PT-RUNTIME-UNIFY-01).
 *
 * Media chrome reads ExperienceHouse media via `experienceHouseMedia.ts`.
 * Runtime HousePackage is the sole object SSOT after Builder import.
 *
 * This module remains only as a documentation stub so import greps fail closed.
 */

export {};
