# ARCHIVAL SNAPSHOT — not Live Runtime

This directory is a **frozen visual / UX reference build**.

- Not Runtime SSOT
- Not day-to-day development (`:4173`)
- Not Embed Demo / Playground
- Not production partner delivery (use `docs/embed` IIFE after release build)

Serve only via the explicit archive command in `REFERENCE.json` → `serve.command`.
