{
  "name": "@embed-engine/conis-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DEV-014 — local development host for the public CONIS website (docs/ → conis.cz)",
  "scripts": {
    "dev": "vite --config vite.config.ts",
    "preview": "vite preview --config vite.config.ts"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
