# Lead capture (optional — without these, leads are stored in data/ and mail is logged)

# SMTP
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# LEAD_EMAIL_TO=kontakt@conis.cz
# LEAD_EMAIL_FROM=noreply@conis.cz

# Google Sheets Apps Script web app URL (doPost JSON)
# Expected JSON keys: timestamp, name, company, email, phone, answers, status, userAgent, ip
# GOOGLE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/.../exec

# PORT=3000
